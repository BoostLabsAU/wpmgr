{
  "name": "@wpmgr/tracker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "MIT-licensed Core Web Vitals tracker (V1). Bundles Google web-vitals (Apache-2.0) into a single IIFE.",
  "scripts": {
    "build": "node build.mjs",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no unit tests for the tracker bundle'"
  },
  "dependencies": {
    "web-vitals": "^5.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.4",
    "typescript": "^5.6.3"
  }
}
