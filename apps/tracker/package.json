{
  "name": "@wpmgr/tracker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "MIT-licensed Core Web Vitals tracker (V1). Bundles Google web-vitals (Apache-2.0) into a single IIFE.",
  "scripts": {
    "build": "node build.mjs",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no unit tests for the tracker bundle'",
    "test:cls": "node tests/cls-beacon.test.mjs"
  },
  "dependencies": {
    "web-vitals": "^5.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.4",
    "playwright": "^1.60.0",
    "typescript": "^5.6.3"
  }
}
